{
  "name": "db-sync-dashboard",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write '**/*.{ts,tsx,md}'"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
